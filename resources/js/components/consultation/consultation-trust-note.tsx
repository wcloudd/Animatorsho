import { CONSULTATION_TRUST_NOTE } from '@/lib/consultation-form-data';

export function ConsultationTrustNote() {
    return (
        <p className="text-center text-xs font-medium leading-relaxed text-muted">
            {CONSULTATION_TRUST_NOTE}
        </p>
    );
}
